#ifndef MEMORY_H
#define MEMORY_H

//Prototype the function. Note that the name of the formal parameter is optional.
int memoryMap(float); //It just needs to know how much space to allocate, doesn't care about the name of the float

#endif // !MEMORY_H


